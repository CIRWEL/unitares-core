[build-system]
requires = ["setuptools>=68.0", "Cython>=3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "unitares-core"
version = "2.3.0"
requires-python = ">=3.12"
dependencies = ["numpy>=1.24.0,<3.0.0"]
